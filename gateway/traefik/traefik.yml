providers:
  docker:
    network: "entask"
    watch: true

api:
  dashboard: true
  insecure: true
  
http:    
  routers:
    auth:
      rule: "PathRegexp(`/api/auth(.*?)`)"
      service: auth-service
    
    version:
      rule: "PathRegexp(`/api/version(.*?)`)"
      service: version-service

log:
  level: DEBUG
  maxBackups: 3